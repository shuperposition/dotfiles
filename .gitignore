# Macos meta files
*.DS_Store

# Development meta files
*.python-version
*.swo
*.swp
*.vim/
tags
Session.vim

# Byte-compiled
__pycache__/
*.class
*.egg-info

# Pytest
.coverage
.pytest*
htmlcov

# Machine-local shell overrides.
# Seeded once from zsh/zshlc.template and edited per machine, so it must never
# be committed and must never be overwritten by the installer.
.zshlc

**/.claude/settings.local.json
